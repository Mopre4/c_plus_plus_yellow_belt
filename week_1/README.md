Џредисловие
…сли вы не уверены, стоит ли вам сначала пройти наш первый курс, или у вас достаточно знаний, чтобы начать с З†Юлтого поЯсаИ, попробуйте 
решить эту задачу. …сли вы не понимаете еЮ условие или не можете быстро решить, скорее всего, вам надо сначала пройти ЗЃелый поЯсИ

“словие
‚ этой задаче вам надо разработать класс Matrix длЯ представлениЯ целочисленной матрицы. Ѓолее подробно, класс Matrix должен иметь:

конструктор по умолчанию, который создаЮт матрицу с нулЮм строк и нулЮм столбцов
конструктор от двух целочисленных параметров: num_rows и num_cols, С которые задают количество строк и столбцов матрицы соответственно
метод Reset, принимающий два целочисленных параметра, которые задают новое количество строк и столбцов матрицы соответственно; метод Reset 
менЯет размеры матрицы на заданные и обнулЯет все еЮ элементы
константный метод At, который принимает номер строки и номер столбца (именно в этом порЯдке; нумерациЯ строк и столбцов начинаетсЯ с нулЯ) 
и возвращает значение в соответствущей Ячейке матрицы
неконстантный метод At с такими же параметрами, но возвращающий ссылку на значение в соответствущей Ячейке матрицы
константные методы GetNumRows и GetNumColumns, которые возвращают количество строк и столбцов матрицы соответственно
…сли количество строк или количество столбцов, переданное в конструктор класса Matrix или метод Reset, оказалось отрицательным, то должно 
быть выброшено стандартное исключение out_of_range.

ќто же исключение должен бросать метод At, если переданнаЯ в него Ячейка выходит за границы матрицы.

Љроме того, длЯ класса Matrix должны быть определены следующие операторы:

оператор ввода из потока istream; формат ввода простой С сначала задаЮтсЯ количество строк и столбцов (именно в этом порЯдке), 
а затем все элементы матрицы: сначала элемент первой строки и первого столбца, затем элемент первой строки и второго столбца и так далее
оператор вывода в поток ostream; он должен выводить матрицу в том же формате, в каком еЮ читает оператор ввода, С в первой строке количество 
строк и столбцов, во второй С элементы первой строки, в третьей С элементы второй строки и т.д.
оператор проверки на равенство (==): он должен возвращать true, если сравниваемые матрицы имеют одинаковый размер и все их соответствующие 
элементы равны между собой, в противном случае он должен возвращать false.
оператор сложениЯ: он должен принимать две матрицы и возвращать новую матрицу, котораЯ ЯвлЯетсЯ их суммой; если переданные матрицы 
имеют разные размеры этот оператор должен выбрасывать стандартное исключение invalid_argument.
‚ам дана заготовка решениЯ, а также пример ввода и вывода. Љогда вы реализуете класс Matrix, вы можете использовать этот пример, 
чтобы убедитьсЯ, что ваш код работает правильно.

‚ажно! Џример не проверЯет выполнение всех требований, приведЮнных в условии.

Џримечание
Њатрицы с нулЮм строк или нулЮм столбцов считаютсЯ пустыми. ‹юбые две пустые матрицы равны. ‘умма двух пустых матриц также ЯвлЯетсЯ пустой матрицей.

Ђвтор задачи С „митрий Њинченко


Џример ввода

		3 5
	6 4 -1 9 8
	12 1 2 9 -5
	-4 0 12 8 6

	3 5
	5 1 0 -8 23
	14 5 -6 6 9
	8 0 5 4 1
Џример вывода

	3 5
	11 5 -1 1 31
	26 6 -4 15 4
	4 0 17 12 7